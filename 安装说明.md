# 绩效评估系统 - 安装部署指南

## 📦 应用程序信息
- **应用名称：** 绩效评估系统
- **版本：** 1.0.0
- **适用平台：** Windows 10/11 (64位)
- **打包时间：** 2025年8月29日

## 🚀 快速安装步骤

### 1. 获取应用程序
- **安装包位置：** `F:\安装包\绩效评估系统-最新版\`
- **主执行文件：** `performance-evaluation-system.exe` (196MB)

### 2. 在其他电脑上安装

#### 方法一：直接复制运行（推荐）
1. **复制整个应用文件夹**
   ```
   将 "绩效评估系统-最新版" 整个文件夹复制到目标电脑
   推荐路径：C:\Program Files\绩效评估系统\
   或者：D:\绩效评估系统\
   ```

2. **运行应用程序**
   ```
   双击 performance-evaluation-system.exe 即可启动
   首次运行会自动创建数据库
   ```

3. **创建桌面快捷方式（可选）**
   - 右键点击 `performance-evaluation-system.exe`
   - 选择"发送到" → "桌面快捷方式"
   - 重命名为"绩效评估系统"

#### 方法二：网络共享部署（多用户环境）
1. **设置服务器共享**
   ```
   将应用程序放在服务器共享文件夹
   例如：\\192.168.1.100\软件\绩效评估系统\
   ```

2. **客户端访问**
   - 映射网络驱动器
   - 创建快捷方式指向网络路径
   - 注意：每个用户的数据库是独立的

## 💾 数据存储说明

### 数据库文件位置
```
Windows路径：C:\Users\[用户名]\AppData\Roaming\performance-evaluation-system\performance.db
```

### 数据库特性
- **类型：** SQLite数据库
- **大小：** 约120KB（空数据库）
- **自动创建：** 首次运行时自动创建
- **表结构：** employees（员工）、items（条目）、records（记录）

### 数据备份策略
1. **自动备份位置**
   ```
   C:\Users\[用户名]\AppData\Roaming\performance-evaluation-system\backups\
   ```

2. **手动备份方法**
   - 复制 `performance.db` 文件
   - 使用应用内"导出"功能
   - 建议备份频率：每周

3. **数据恢复方法**
   - 替换 `performance.db` 文件
   - 重新导入Excel数据

## ⚙️ 系统要求

### 最低配置
| 项目 | 要求 |
|------|------|
| 操作系统 | Windows 10 64位 |
| 内存 | 4GB RAM |
| 硬盘 | 500MB 可用空间 |
| 处理器 | 双核处理器 |
| 网络 | 可选（AI匹配功能需要） |

### 推荐配置
| 项目 | 推荐 |
|------|------|
| 操作系统 | Windows 11 64位 |
| 内存 | 8GB RAM |
| 硬盘 | 1GB 可用空间 |
| 处理器 | 四核处理器 |
| 网络 | 稳定的互联网连接 |

## 🔧 功能模块说明

### 1. 员工管理 (`/`)
- **功能：** 员工基础信息管理
- **操作：** 新增、编辑、删除、批量导入/导出
- **格式要求：** 工号必须为6位数字

### 2. 绩效管理办法 (`/items`)
- **功能：** 考核条目和评分标准管理
- **操作：** 条目设置、分值配置、状态管理
- **支持：** Excel批量导入，按纸质编号排序

### 3. 绩效登记 (`/entry`)
- **功能：** 考核记录录入和智能匹配
- **特色功能：**
  - 手动录入考核记录
  - Excel批量导入
  - AI智能匹配（需配置API）
  - JSON数据交换

### 4. 数据导出 (`/export`)
- **功能：** 绩效数据汇总和报表生成
- **导出格式：**
  - 车间格式（管理用）
  - 系统格式（分析用）
- **筛选功能：** 按员工、月份筛选

### 5. API管理 (`/api-management`)
- **功能：** AI接口配置管理
- **访问密码：** `!nbdt2025`
- **配置项：** API URL、工作流ID、访问令牌
- **配置文件位置：** `C:\Users\[用户名]\AppData\Roaming\performance-evaluation-system\config.env`

## 🤖 AI智能匹配配置

### 配置步骤
1. **进入API管理页面**
   - 点击左侧菜单"API管理"
   - 输入管理员密码：`!nbdt2025`

2. **填写配置信息**
   - **API URL：** `https://api.coze.cn/v1/workflow/stream_run`
   - **Workflow ID：** 从Coze平台获取的工作流ID
   - **Personal Access Token：** 从Coze平台获取的API密钥

3. **保存并重启**
   - 点击"保存配置"
   - 重启应用使配置生效

### 获取API密钥
1. 访问 [Coze官网](https://www.coze.cn/)
2. 注册/登录账号
3. 创建工作流
4. 获取API密钥和工作流ID

### 配置文件说明
- **位置：** 用户数据目录下的`config.env`文件
- **格式：** 键值对格式，如`VITE_COZE_API_KEY=your_key_here`
- **安全：** 配置文件包含敏感信息，请妥善保管

## 📋 使用流程指南

### 初次部署流程
```
1. 复制应用程序到目标电脑
   ↓
2. 启动应用程序（自动创建数据库）
   ↓
3. 导入员工基础数据
   ↓
4. 导入绩效考核条目
   ↓
5. 开始日常绩效登记工作
```

### 日常操作流程
```
考核事件发生 → 登记考核记录 → 智能匹配条目 → 生成绩效报表
```

## 🛡️ 安全和维护

### 数据安全
- **权限控制：** 建议限制数据库文件访问权限
- **备份策略：** 每周备份数据库文件
- **敏感信息：** API密钥请妥善保管

### 维护建议
- **定期更新：** 关注应用程序更新
- **性能监控：** 定期检查数据库大小
- **清理工作：** 定期清理过期的考核记录

## 🔄 数据迁移

### 从旧系统迁移
1. **导出旧系统数据为Excel格式**
2. **按照模板格式整理数据**
3. **使用批量导入功能导入数据**

### 多台电脑间数据同步
1. **方法一：** 复制数据库文件
2. **方法二：** 导出Excel后在另一台电脑导入
3. **方法三：** 使用网络共享数据库（需技术支持）

## ❗ 常见问题解决

### 启动问题
| 问题 | 解决方案 |
|------|----------|
| 应用无法启动 | 安装 Visual C++ Redistributable |
| 权限不足 | 以管理员身份运行 |
| 缺少文件 | 重新复制完整的应用文件夹 |

### 导入问题
| 问题 | 解决方案 |
|------|----------|
| Excel导入失败 | 检查工号格式（6位数字） |
| 数据不显示 | 刷新页面或重启应用 |
| 格式错误 | 下载最新模板重新整理数据 |

### 性能问题
| 问题 | 解决方案 |
|------|----------|
| 运行缓慢 | 清理过期数据，重启应用 |
| 内存占用高 | 关闭不必要的程序 |
| 数据库过大 | 导出数据后清理历史记录 |

## 📞 技术支持

### 联系方式
- **技术支持：** 请联系系统管理员
- **问题反馈：** 提供错误截图和操作步骤

### 日志收集
如遇问题，请提供：
1. **错误截图**
2. **浏览器控制台日志**（按F12查看）
3. **操作步骤描述**
4. **系统环境信息**

---

**© 2025 绩效评估系统 v1.0.0**  
**最后更新：2025年8月29日** 